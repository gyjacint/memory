package xml;

import static org.junit.Assert.*;
import game.Player;

import java.util.List;

import org.junit.Test;

public class XMLMultiStatHandlerTest {

	@Test
	public void testReadXML() {
		XMLMultiStatHandler handler = new XMLMultiStatHandler();
//		List<Player> list = handler.readXML("multitest.xml");
//		assertEquals("Jácint", list.get(0).getName());
//		assertEquals(15, list.get(0).getWin());
//		assertEquals(0, list.get(1).getTie());
//		assertEquals(1, list.get(2).getLose());
	}

}
